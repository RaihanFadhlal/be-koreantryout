spring.application.name=tekor

#DB
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

#JWT
jwt.secret=${JWT_SECRET}
jwt.expiration.access-token=36000
jwt.expiration.refresh-token=${JWT_EXPIRATION_REFRESH_TOKEN}

# Email Verification
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=raaflahar@gmail.com
spring.mail.password=xjkocjjalhmztfxy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:5173, http://10.10.102.130:8082,http://10.10.102.46:8082, http://localhost:8082

#Server Port
server.port=8081

#App Path
app.base-url=http://localhost:8081
app.frontend.url=http://localhost:5173
app.verification-path=/api/v1/auth/verify
app.reset-password-path=/api/v1/auth/reset-password
app.frontend.verification-handler-url=http://localhost:5173/verify-email

# File Storage
file.upload-dir=./uploads/

# File Size Limit
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=2MB

#Cloudinary
cloudinary.cloud_name=de7fcoe98
cloudinary.api_key=${CLOUDINARY_API_KEY}
cloudinary.api_secret=${CLOUDINARY_API_SECRET}

#Midtrans
midtrans.server.key=${MIDTRANS_SERVER_KEY}
midtrans.client.key=${MIDTRANS_CLIENT_KEY}
midtrans.is.production=false

#Gemini API
google.api.key=${GOOGLE_API_KEY}

#Vertex API
gcp.project.id=${GOOGLE_CLOUD_PROJECT}
gcp.location=asia-southeast1
